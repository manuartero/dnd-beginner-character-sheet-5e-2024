[] Prepare Spells (Spell Book): scroll into view after clicking a prepare button so the unrolled grid is visible; scrollIntoView on the button itself is a no-op since the button is already visible — need to scroll the newly rendered grid container instead
[] Equipment (Character Sheet): Initial Equipment in the Character Sheet
[] Equipment (Character Creation), choose two options for starting equipment (origin starting equipment or extra gold); add selected option to character equipment
[] Export/Import: feat; export character as .json file - import character as json file.
[] Abilities Step (Character Sheet): Add saving throws to info (needed design)
